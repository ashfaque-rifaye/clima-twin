# --- Gemini API (Google AI Studio, FREE Flash tier) ---
# Create a key at https://aistudio.google.com/apikey  (free tier; do NOT enable Pro models)
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash

# --- Google Maps Platform ---
# Restrict this key to your domain + only the APIs you enable
GOOGLE_MAPS_API_KEY=

# --- Google Cloud ---
GCP_PROJECT=
BQ_DATASET=climatwin
BQ_LOCATION=asia-south1

# --- CORS (frontend origin) ---
CORS_ORIGINS=http://localhost:5173
